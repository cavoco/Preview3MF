## Xcode
build/
DerivedData/
*.xcuserdata
xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
# Package.resolved is deliberately tracked: it pins the exact Sparkle and
# ZIPFoundation builds that ship, so a release can't pick up a new upstream
# version without a commit saying so.

## macOS
.DS_Store
.AppleDouble
.LSOverride

## Claude
.claude/

# Local test models (third-party, not ours to redistribute)
*.3mf
!Preview3MFTests/Fixtures/*.3mf
